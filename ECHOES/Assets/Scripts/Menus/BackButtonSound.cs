using UnityEngine;

namespace Echoes.Menus
{
    // Marcador puro sin lógica propia: ponlo en cualquier botón que funcionalmente sea "volver"
    // (p. ej. el Volver de Opciones) para que UISoundManager reproduzca el sonido de cancelar en
    // vez del de confirmar genérico al pulsarlo, con ratón, teclado o mando.
    public class BackButtonSound : MonoBehaviour
    {
    }
}
